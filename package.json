{
  "name": "rxjs-websocket-server-subject",
  "version": "1.0.0",
  "description": "RxJs WebSocket Server Wrapper for Node.js using WS",
  "main": "dist/WebSocketServerSubject.js",
  "module": "dist/WebSocketServerSubject.es.js",
  "types": "dist/WebSocketServerSubject.d.ts",
  "files": ["dist"],
  "repository": "https://github.com/six-edge/rxjs-websocket-server-subject.git",
  "author": "Alexander Barker <alex@sixedge.com>",
  "license": "MIT",
  "scripts": {
    "build": "tsc && yarn renameES && yarn renameMap && tsc --module CommonJS",
    "renameES": "mv dist/WebSocketServerSubject.js dist/WebSocketServerSubject.es.js",
    "renameMap": "mv dist/WebSocketServerSubject.js.map dist/WebSocketServerSubject.es.js.map"
  },
  "dependencies": {
    "rxjs": "^6.6.3"
  },
  "devDependencies": {
    "typescript": "^4.1.3"
  }
}
